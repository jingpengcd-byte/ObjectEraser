# LaMa图像修复演示系统安装与配置指南

## 1. 系统环境准备

### 1.1 Ubuntu/Debian系统

```bash
# 更新系统包管理器
sudo apt-get update

# 安装Python和相关工具
sudo apt-get install -y \
    python3 \
    python3-pip \
    python3-venv \
    git \
    curl \
    wget \
    build-essential

# 安装图像处理依赖
sudo apt-get install -y \
    libgl1-mesa-glx \
    libglib2.0-0 \
    libsm6 \
    libxrender1 \
    libxext6

# 验证Python安装
python3 --version
pip3 --version
```

### 1.2 CentOS/RHEL系统

```bash
# 安装必要工具
sudo yum install -y \
    python3 \
    python3-pip \
    git \
    curl \
    wget \
    gcc \
    gcc-c++ \
    make

# 创建Python虚拟环境
python3 -m venv /opt/lama-demo/venv
source /opt/lama-demo/venv/bin/activate
```

### 1.3 macOS系统

```bash
# 使用Homebrew安装依赖
brew install python3 git curl wget

# 安装Python虚拟环境
python3 -m venv venv
source venv/bin/activate
```

## 2. LaMa项目准备

### 2.1 克隆LaMa项目

```bash
# 进入工作目录
cd /home/guojingpeng/workSpace

# 克隆LaMa项目
git clone https://github.com/advimman/lama.git

# 验证克隆结果
ls -la lama/
```

### 2.2 项目结构验证

确保以下文件和目录存在：
```
lama/
├── bin/                    # 可执行脚本
├── configs/               # 配置文件
├── saicinpainting/        # 核心算法
├── requirements.txt      # Python依赖
├── conda_env.yml         # Conda环境
└── README.md            # 项目文档
```

## 3. 演示系统安装

### 3.1 基本安装

```bash
# 进入演示目录
cd /home/guojingpeng/workSpace/demo

# 运行初始设置
chmod +x start.sh stop.sh
./start.sh setup
```

### 3.2 手动安装步骤

如果自动安装失败，可以手动执行以下步骤：

#### 步骤1：创建虚拟环境
```bash
cd /home/guojingpeng/workSpace/demo
python3 -m venv venv
source venv/bin/activate
```

#### 步骤2：安装LaMa依赖
```bash
cd /home/guojingpeng/workSpace/lama
pip install --upgrade pip
pip install -r requirements.txt
```

#### 步骤3：安装演示系统依赖
```bash
pip install flask flask-cors opencv-python pillow numpy
```

#### 步骤4：安装PyTorch

**CPU版本**：
```bash
pip install torch torchvision torchaudio
```

**GPU版本（CUDA 11.8）**：
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

**GPU版本（CUDA 10.2）**：
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu102
```

#### 步骤5：验证安装
```bash
python3 -c "import torch; print(f'PyTorch版本: {torch.__version__}')"
python3 -c "import flask; print(f'Flask版本: {flask.__version__}')"
```

## 4. 模型下载与配置

### 4.1 下载预训练模型

#### 方法1：从Google Drive下载
```bash
# 进入LaMa项目目录
cd /home/guojingpeng/workSpace/lama

# 手动下载big-lama.zip
# 访问: https://drive.google.com/drive/folders/1B2x7eQDgecTL0oh3LSIBDGj0fTxs6Ips

# 解压模型文件
unzip big-lama.zip

# 验证模型文件
ls -la big-lama/
```

#### 方法2：从Hugging Face下载
```bash
cd /home/guojingpeng/workSpace/lama

# 使用curl下载
curl -LJO https://huggingface.co/smartywu/big-lama/resolve/main/big-lama.zip

# 解压
unzip big-lama.zip
rm big-lama.zip

# 验证
ls big-lama/models/
```

#### 方法3：使用备用下载脚本
```bash
cd /home/guojingpeng/workSpace/lama

# 创建下载脚本
cat > download_model.sh << 'EOF'
#!/bin/bash
echo "正在下载LaMa模型..."
echo "请手动从以下地址下载模型:"
echo "1. https://drive.google.com/drive/folders/1B2x7eQDgecTL0oh3LSIBDGj0fTxs6Ips"
echo "2. https://huggingface.co/smartywu/big-lama"
echo ""
echo "下载后执行:"
echo "unzip big-lama.zip"
echo "mv big-lama /home/guojingpeng/workSpace/lama/"
EOF

chmod +x download_model.sh
./download_model.sh
```

### 4.2 模型配置

#### 检查模型路径
```bash
# 确认模型路径正确
ls /home/guojingpeng/workSpace/lama/big-lama/

# 应有以下结构:
# big-lama/
# ├── config.yaml
# ├── models/
# │   └── best.ckpt
# └── 其他训练文件
```

#### 配置示例
如果模型下载失败，可以创建示例配置：

```bash
cd /home/guojingpeng/workSpace/lama

# 创建示例模型目录
mkdir -p big-lama/models

# 创建配置文件
cat > big-lama/config.yaml << 'EOF'
model:
  path: /home/guojingpeng/workSpace/lama/big-lama
  checkpoint: best.ckpt
  
training:
  batch_size: 1
  learning_rate: 0.0001
  
data:
  img_size: 256
  mask_range: [0.1, 0.5]
EOF

# 创建空检查点文件
touch big-lama/models/best.ckpt
```

## 5. 系统启动

### 5.1 测试启动

```bash
# 进入演示目录
cd /home/guojingpeng/workSpace/demo

# 测试启动（如果提示端口占用，会自动使用5001端口）
./start.sh
```

### 5.2 检查服务状态

```bash
# 查看服务是否正常运行
curl http://localhost:5000/api/status

# 预期输出:
# {"status":"ok","model_loaded":true,"device":"cuda","has_cuda":true}
```

### 5.3 访问Web界面

打开浏览器访问：
- `http://localhost:5000` （默认端口）
- `http://localhost:5001` （如果5000被占用）

## 6. 高级配置

### 6.1 调整系统参数

编辑配置文件 `/home/guojingpeng/workSpace/demo/config.py`：

```python
class Config:
    # 上传设置
    MAX_CONTENT_LENGTH = 100 * 1024 * 1024  # 100MB
    UPLOAD_FOLDER = '/data/lama/uploads'
    RESULT_FOLDER = '/data/lama/results'
    ALLOWED_EXTENSIONS = {'png', 'jpg', 'jpeg'}
    
    # 模型设置
    MODEL_PATH = '/home/guojingpeng/workSpace/lama/big-lama'
    
    # 处理设置
    MAX_IMAGE_SIZE = 4096  # 最大图像尺寸
    ENABLE_REFINE = True   # 启用精炼算法
    REFINE_ITERATIONS = 20 # 精炼迭代次数
    
    # 服务器设置
    HOST = '0.0.0.0'
    PORT = 8080
    DEBUG = False
    THREADED = True
```

### 6.2 自定义模型路径

如果需要使用不同的模型路径：

```bash
# 设置环境变量
export LAMA_MODEL_PATH="/path/to/your/model"

# 或在启动脚本中指定
./start.sh --model-path "/path/to/your/model"
```

### 6.3 配置GPU设备

```bash
# 指定GPU设备（多GPU环境）
export CUDA_VISIBLE_DEVICES="0,1"

# 检查GPU状态
nvidia-smi
```

## 7. 系统优化

### 7.1 内存优化

```bash
# 调整虚拟内存
sudo sysctl -w vm.overcommit_memory=1
sudo sysctl -w vm.swappiness=10

# 清理系统缓存
sync && echo 3 | sudo tee /proc/sys/vm/drop_caches
```

### 7.2 性能监控

安装监控工具：
```bash
# 安装htop
sudo apt-get install -y htop

# 监控系统资源
htop

# 监控GPU使用（如有）
watch -n 1 nvidia-smi
```

## 8. 故障排除

### 8.1 常见问题及解决方案

#### 问题1：Python依赖安装失败
```bash
# 升级pip
python3 -m pip install --upgrade pip

# 清理缓存
pip cache purge

# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

#### 问题2：PyTorch安装失败
```bash
# 卸载现有版本
pip uninstall torch torchvision -y

# 根据系统选择安装命令
# CPU版本
pip install torch torchvision

# 指定版本
pip install torch==1.13.1 torchvision==0.14.1
```

#### 问题3：端口被占用
```bash
# 查看占用端口的进程
sudo lsof -i :5000

# 杀死占用进程
sudo kill -9 <PID>

# 或使用其他端口
./start.sh --port 8080
```

#### 问题4：模型加载失败
```bash
# 检查模型文件
ls -la /home/guojingpeng/workSpace/lama/big-lama/

# 检查文件权限
ls -la /home/guojingpeng/workSpace/lama/big-lama/models/

# 重新下载模型
cd /home/guojingpeng/workSpace/lama
rm -rf big-lama
# 重新下载...
```

### 8.2 日志分析

```bash
# 查看实时日志
tail -f /home/guojingpeng/workSpace/demo/demo.log

# 查找错误信息
grep -i error /home/guojingpeng/workSpace/demo/demo.log

# 查看警告信息
grep -i warning /home/guojingpeng/workSpace/demo/demo.log

# 查看启动信息
grep -i starting /home/guojingpeng/workSpace/demo/demo.log
```

### 8.3 性能测试

```bash
# 测试API响应时间
time curl -s http://localhost:5000/api/status > /dev/null

# 测试图像上传
curl -X POST -F "file=@test.jpg" http://localhost:5000/api/upload

# 监控内存使用
watch -n 1 "free -h | grep -E 'Mem|Swap'"
```

## 9. 生产环境部署

### 9.1 使用Gunicorn

```bash
# 安装Gunicorn
pip install gunicorn

# 启动服务（生产环境）
gunicorn --bind 0.0.0.0:5000 --workers 4 --threads 2 --timeout 120 app:app
```

### 9.2 配置Nginx反向代理

创建Nginx配置文件 `/etc/nginx/sites-available/lama-demo`：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 静态文件服务
    location /static {
        alias /home/guojingpeng/workSpace/demo/static;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # 动态请求代理
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

启用站点：
```bash
sudo ln -s /etc/nginx/sites-available/lama-demo /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 9.3 设置系统服务

创建systemd服务文件 `/etc/systemd/system/lama-demo.service`：

```ini
[Unit]
Description=LaMa Demo Web Application
After=network.target

[Service]
User=guojingpeng
Group=guojingpeng
WorkingDirectory=/home/guojingpeng/workSpace/demo
Environment="PATH=/home/guojingpeng/workSpace/demo/venv/bin"
ExecStart=/home/guojingpeng/workSpace/demo/venv/bin/gunicorn \
    --bind 0.0.0.0:5000 \
    --workers 4 \
    --threads 2 \
    --timeout 120 \
    app:app
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启动服务：
```bash
sudo systemctl daemon-reload
sudo systemctl enable lama-demo
sudo systemctl start lama-demo
sudo systemctl status lama-demo
```

## 10. 备份与恢复

### 10.1 数据备份

```bash
# 备份模型文件
tar -czf lama-models-backup-$(date +%Y%m%d).tar.gz \
    /home/guojingpeng/workSpace/lama/big-lama/

# 备份配置文件
tar -czf lama-config-backup-$(date +%Y%m%d).tar.gz \
    /home/guojingpeng/workSpace/demo/config.py \
    /home/guojingpeng/workSpace/demo/start.sh \
    /home/guojingpeng/workSpace/demo/stop.sh
```

### 10.2 系统恢复

```bash
# 恢复模型文件
tar -xzf lama-models-backup-YYYYMMDD.tar.gz -C /

# 恢复配置文件
tar -xzf lama-config-backup-YYYYMMDD.tar.gz -C /

# 重新启动服务
cd /home/guojingpeng/workSpace/demo
./start.sh
```

---

*文档版本: v1.0*  
*最后更新: 2026-03-21*  
*适用系统: LaMa演示系统 v1.0*